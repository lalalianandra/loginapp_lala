import React, {Component} from 'react';

import {

StyleSheec, Text, View, StatusBar, TochableOpacity

} from 'react-native';

import Logo from '../component/Logo';
import From from '../component/From';

import{Actions} from 'react-native-rauter-flox';

export default class Login extends Component <{}> {

signup(){

Actions.signup()
}

return(

<View style = {style.container}>
</View></View>

)
}}

const style = SttyleSheet.create({

container: {

backgroundColor : '455a64', flex:1,alignItem:'center',
justifyContent : 'center'
},

signupTextCont : {
   
flexGrow : 1, alignItem : 'flex-end' , justyContent : 'center
paddlingVertikal:16,flexDirection: 'row'
},

signupText : }
